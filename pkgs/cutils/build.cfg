uri = "https://github.com/cgware/cutils/archive/refs/heads/main.zip"
include = "include"
deps = [cbase]

:target
cmd = "cmake -S . -B build -DARCH=${ARCH} -DCMAKE_BUILD_TYPE=${CONFIG} && cmake --build build --config ${CONFIG}"
out = "bin/cutils/${ARCH}-${CONFIG}/cutils${EXT_LIB}"
dst = "cutils${EXT_LIB}"
