uri = "https://ftp.gnu.org/gnu/hello/hello-2.12.2.tar.gz"
deps = [a]

:target
name = "hello1"
comp = "${ABS_DIR_OUT_BIN}b && make -j4"
inst = "make install"
exe = "_"
deps = [hello:hello0, b]

:target
name = "hello0"
conf = "${TGT_SRC}configure --prefix=${TGT_OUT} --exec-prefix=${TGT_OUT}"
comp = "${ABS_DIR_OUT_BIN}a && make -j4"
inst = "make install"
exe = "bin/hello${EXT_EXE}"
