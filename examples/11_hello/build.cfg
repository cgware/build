uri = "https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz"

:target
conf = "${TGT_SRC}configure --prefix=${ABS_DIR_OUT_EXT_PKG} --exec-prefix=${ABS_DIR_OUT_EXT_PKG}"
comp = "make -j4"
inst = "make install"
exe = "bin/hello${EXT_EXE}"
