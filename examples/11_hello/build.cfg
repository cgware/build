uri = "https://ftp.gnu.org/gnu/hello/hello-2.12.tar.gz"

:target
cmd = "./configure --prefix=${ABS_DIR_OUT_EXT_PKG} --exec-prefix=${ABS_DIR_OUT_EXT_PKG} && make -j4 && make install"
out = "bin/binutils/${ARCH}-${CONFIG}/binutils${EXT_LIB}"
dst = "binutils${EXT_LIB}"
