uri = "https://github.com/cgware/cbase/archive/refs/heads/main.zip"
include = "include"

:target
cmd = "cmake -S . -B build -DARCH=${ARCH} -DCMAKE_BUILD_TYPE=${CONFIG} && cmake --build build --config ${CONFIG}"
out = "bin/cbase/${ARCH}-${CONFIG}/cbase${EXT_LIB}"
dst = "cbase${EXT_LIB}"
