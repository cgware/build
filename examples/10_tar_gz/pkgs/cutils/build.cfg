uri = "https://github.com/cgware/cutils/archive/refs/heads/main.tar.gz"
include = "include"
deps = [cbase]

:target
cmd = "cmake -S . -B build/${ARCH} ${ARGS_ARCH} -DARCH=${ARCH} -DCMAKE_BUILD_TYPE=${CONFIG} && cmake --build build/${ARCH} --config ${CONFIG}"
out = "bin/cutils/${ARCH}-${CONFIG}/cutils${EXT_LIB}"
dst = "cutils${EXT_LIB}"
